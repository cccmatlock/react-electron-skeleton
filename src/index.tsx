import ReactDOM from "react-dom";
import { App } from "./App";

const Root: React.FC = () => (
	<div>
		What up
		<h1>We in this</h1>
	</div>
);

ReactDOM.render(<Root />, document.getElementById("root"));
